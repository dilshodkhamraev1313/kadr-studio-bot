name: Keep bot awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf https://kadr-studio-bot.onrender.com/ || echo "Ping failed (bot may be cold-starting)"
